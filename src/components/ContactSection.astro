---
// No props needed for now
---
<section class="contact">
  <h2>Kontaktujte nás</h2>
  <p>Máte zájem o řešení nebo potřebujete více informací?</p>
  <p style="margin: 1.5rem 0; font-size: 1.2rem; font-weight: 500; color: #A9D6E5; user-select: all;"><a href="mailto:info@azvision.cz" style="color: inherit; text-decoration: none;">info@azvision.cz</a></p>
  <p style="font-size: 1.2rem; color: var(--accent-color); margin-bottom: 2rem;"><a href="tel:+420725398765" style="color: inherit; text-decoration: none;">+420 725 398 765</a></p>
  <form
    action="https://formspree.io/f/xblklkvy"
    method="POST"
  >
    <label>
      Váš e-mail:
      <input type="email" name="email">
    </label>
    <label>
      Telefon:
      <input type="tel" name="phone">
    </label>
    <label>
      Zpráva pro nás:
      <textarea name="message"></textarea>
    </label>
        <button type="submit" class="contact-btn">Odeslat</button>
  </form>
</section>

<style>
  form {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    width: 100%;
    max-width: 500px;
    margin: 2rem auto 0;
  }

  label {
    display: flex;
    flex-direction: column;
    font-size: 0.9rem;
    color: #c5c9d3;
  }

  input[type="email"],
  input[type="tel"],
  textarea {
    background-color: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 8px;
    padding: 0.75rem 1rem;
    color: #ffffff;
    font-size: 1rem;
    margin-top: 0.5rem;
    transition: all 0.2s ease-in-out;
  }

  input[type="email"]:focus,
  input[type="tel"]:focus,
  textarea:focus {
    outline: none;
    border-color: var(--accent-color);
    box-shadow: 0 0 0 3px rgba(169, 214, 229, 0.3);
  }

  textarea {
    min-height: 120px;
    resize: vertical;
  }

  form button.contact-btn {
    border: none;
    cursor: pointer;
    width: auto;
    align-self: flex-start;
  }
</style>
